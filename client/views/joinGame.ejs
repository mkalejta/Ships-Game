<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <style>
        form {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        form label {
            font-weight: bold;
        }

        form input {
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container text-center mx-auto">
        <h2>Join Game</h2>
    
        <form action="" method="PUT">
            <label for="nameInput">Name: </label>
            <input type="text" id="nameInput" name="joiner">
    
            <button class="btn btn-primary" style="font-weight: bold; font-size: 1rem;" type="submit">Join Game</button>
        </form>
    </div>
    <script src="/js/API.js"></script>
    <script src="/js/utils.js"></script>
    <script src="/js/actions.js"></script>
    <script>
        let Id;

        const fullUrl = window.location.href;
        const urlParts = fullUrl.split('/');
        const gameIndex = urlParts.indexOf('game');
        if (gameIndex !== -1 && urlParts[gameIndex + 1]) {
            Id = urlParts[gameIndex + 1];
        }

        function actionPath() {
            document.querySelector('form').action = `/api/game/${Id}/join`;
            return true;
        }

        actionPath()
    </script>
</body>
</html>