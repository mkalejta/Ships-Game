
export default class Ship {
    constructor(size, start, end) {
        this.size = size
        this.start = start
        this.end = end
        this.hits = []
        this.sink = false
    }

    // funkcja ifHit()

    // funkcja ifSink()
}